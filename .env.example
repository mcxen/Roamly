# 服务端口
PORT=4173

# local 或 webdav
STORAGE_DRIVER=local

# 本地地图目录（local 模式可选，不填可在网页端设置）
MAP_LIBRARY_DIR=/Users/mcx/Documents/MapLibrary

# SQLite 路径
DB_PATH=./server/data/roamly.db

# 是否监听目录变化（仅 local 模式有效）
WATCH_LIBRARY=true

# WebDAV 配置（webdav 模式必填）
# WEBDAV_URL=https://dav.example.com/remote.php/webdav
# WEBDAV_USER=your_user
# WEBDAV_PASS=your_pass

# 生产环境前端静态文件目录
WEB_DIST_DIR=./web/dist

# OCR 配置（需要本机安装 tesseract）
OCR_ENABLED=true
OCR_LANG=chi_sim+eng

# 推荐目录规范（用于自动分类和城市定位）
# national/中国/省/市/文件名.jpg
# international/国家/省州/城市/文件名.jpg
